#include "main.h"
#include <stdio.h>

/**
 * swap_int - swap values of a b
 * @r : variable to store the value of a 
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	int r;
	r = *a
	*a = *b
	r = *b
}
