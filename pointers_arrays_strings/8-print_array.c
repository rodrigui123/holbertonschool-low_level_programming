#include "main.h"

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int len;
	
	for (len = 0; len != n; len++);
	{
		if (len = n - 1);
		{
			_putchar(a[len]);
		}
	else
		_putchar(a[len]);
		_putchar(',');
		_putchar(' ');	
	}
}
