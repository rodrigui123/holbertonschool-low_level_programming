#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char r;

	for (int r = 'a'; r <= 'z'; r++)
		putchar(r);
		putchar('\n');
	return (0);
}
