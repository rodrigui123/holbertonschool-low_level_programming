#include "main.h"
/**
* print_alphabet - print_alphabet will print the whole alphabet
*
* Return: Always 0 (Success)
*/

int _islower(int c)
{
	if (c > 'a' && c <= 'z')
	       return (1);
	{
	return (0);
	}
}
