#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies the variables
 * @a : one of the integers
 * @b : the other integer
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int s;

	s = a * b;
	return (s);
}
