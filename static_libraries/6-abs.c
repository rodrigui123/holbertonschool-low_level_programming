#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 * @i : abs variable
 * Return: Always 0.
 */
int _abs(int i)
{
	i = abs(i);
	return (i);
}
