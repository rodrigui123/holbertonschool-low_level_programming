#include "lists.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
list_t *add_node(list_t **head, const char *str)
{
	struct *head new_node = (struct *head) malloc(sizeof(struct head));

	new_node-> = str;
	new_node->next = (headnode);
	(headnode) = newnode;
}
